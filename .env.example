# Copy to .env. Never commit .env — it is gitignored.

# --- Ideation ------------------------------------------------------------
# https://console.anthropic.com/
ANTHROPIC_API_KEY=

# --- Metricool -----------------------------------------------------------
# API access requires a Metricool Advanced plan.
# The token is under Settings; userId and blogId appear in any dashboard URL.
METRICOOL_TOKEN=
METRICOOL_USER_ID=
METRICOOL_BLOG_ID=

# Set these ONLY after `reel probe` tells you what the trial-reel field is
# called. Left unset, posts are scheduled as ordinary reels.
# METRICOOL_TRIAL_FIELD=
# METRICOOL_TRIAL_VALUE=true

# --- Media hosting -------------------------------------------------------
# Metricool fetches media over HTTP, so rendered mp4s must sit at a public URL.
# Point this at the directory you upload `out/` into.
MEDIA_PUBLIC_BASE_URL=

# --- Optional ------------------------------------------------------------
# Stock b-roll fill for `reel render --stock`. https://www.pexels.com/api/
# PEXELS_API_KEY=

# Font used for the burned-in hook. Must be installed locally.
# REEL_FONT_FAMILY="Helvetica Neue, Arial, sans-serif"
